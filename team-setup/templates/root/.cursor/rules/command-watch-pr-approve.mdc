---
description: "Arm a background watcher for human GitHub Approve or Request changes on one or more PRs (`owner/repo#N`). On Approve: triage + merge. On Request changes: fix path (no merge). Follow the `pr-approve-watch` skill."
alwaysApply: false
---

# /watch-pr-approve (Cursor)

Arm a background watcher for human GitHub **Approve** or **Request changes** on
one or more PRs (`owner/repo#N`). Wake JSON includes `signal`: `approved` or
`changes_requested`. Follow the `pr-approve-watch` skill
(`.claude/skills/pr-approve-watch/SKILL.md` or
`.cursor/skills/pr-approve-watch/SKILL.md`). Pass PR refs from the user or
from PRs you just opened.
