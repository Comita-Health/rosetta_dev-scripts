Publish the dated sandbox verify list to Slack **Sandbox verify** and background-watch Bret’s Verified / Failed check-offs. Follow the `stakeholder-verify-watch` skill. Bret has no GitHub — do not make Russ relay. Pair with `/watch-deploy-verify` and `/watch-pr-approve`.

Note: chat wake notify is best-effort — drain `AGENT_LOOP_WAKE_stakeholder_verify` from the watcher terminal if the chat stays quiet.
