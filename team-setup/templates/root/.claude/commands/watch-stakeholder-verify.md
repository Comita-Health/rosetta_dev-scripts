Publish the dated release note’s **Not verified** lines as one Slack thread in **#comita-support** — root message `@channel`, one reply per smoke line, Bret reacts :white_check_mark: verified / :x: failed. Publish only after the sandbox deploy for that SHA is green. Follow the `stakeholder-verify-watch` skill. Do **not** arm a local Slack poller — Slack is the live ledger; GHA comments :x: rows onto the Ship issue; promote snapshots git. Bret has no GitHub — do not make Russ relay. Pair with `/watch-deploy-verify` and `/watch-pr-approve`.
